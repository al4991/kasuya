import React, { useState } from "react";

const Pours = () => {
  const [total, setTotal] = useState(500);
  const [amounts, setAmount] = useState([100, 100, 100, 100, 100]);
  const [dimmed, setDimmed] = useState([false, false, false, false, false]);

  return <div></div>;
};

export default Pours;
